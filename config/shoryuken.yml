concurrency: 200
delay: 0
pidfile: tmp/pids/shoryuken.pid

groups:
  events:
    concurrency: 95
    queues:
      - events
      - events_reindex_daily
  events_index:
    concurrency: 95
    queues:
      - events_index
